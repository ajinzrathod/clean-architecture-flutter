name: clean_architecture_1
description: Power Up Your Flutter Development Process by Implementing Clean Architecture and Test-Driven Development

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  http:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  get_it:
  flutter_bloc:
  equatable:
  dartz:
  connectivity:
  shared_preferences:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  mockito:
  build_runner:

flutter:
  uses-material-design: true